#include <iostream>
#include <iomanip>
#include <ctime>
#include <cmath>

int main()
{
    int a, b;
    std::cin >> a >> b;
    int result = 1;
    for (int i = a; i <= b; i++)
    {
        result *= i;
    }
    std::cout << result;
    return 0;
}
