#include <iostream>
#include <iomanip>
#include <ctime>
#include <cmath>

int main()
{
    int n;
    std::cin >> n;
    int sum = 0;
    for (int i = 0; i <= n; i++)
    {
        sum += (n + i)*(n + i);
    }
    std::cout << sum;
    return 0;
}
