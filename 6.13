#include <iostream>
#include <iomanip>
#include <cmath>

int main()
{
    int n;
    std::cin >> n;
    double result = 0;
    int a = -1;
    for (int i = 1; i < n; i++)
    {
        a *= -1;
        result += a * (1 + 0.1 * i);
    }
    std::cout << result;
    return 0;
}
